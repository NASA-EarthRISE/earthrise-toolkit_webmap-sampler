SECRET_KEY=change-me-to-a-long-random-string
DEBUG=False
ALLOWED_HOSTS=earthrise-internal.sciencecloud.nasa.gov

# Sub-path deployment (e.g. /webmap-sampler in production, empty for local dev)
# Leave empty when running locally; set to /webmap-sampler on the NASA proxy.
SCRIPT_NAME=/webmap-sampler

CSRF_TRUSTED_ORIGINS=https://earthrise-internal.sciencecloud.nasa.gov
